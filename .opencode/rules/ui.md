# UI 규칙

- UI 변경은 기존 shadcn-admin 패턴을 우선 따른다.
- 상호 작용은 단순한 구조로 유지한다.
- 데스크톱과 모바일에서 모두 깨지지 않게 본다.
- 폼, 테이블, 다이얼로그, 드로어는 기존 패턴을 우선 따른다.
- 접근성 없는 UI는 완료로 보지 않는다.
- 공용 컴포넌트와 폼 위젯은 대응하는 단위 테스트 파일을 함께 계획한다.
