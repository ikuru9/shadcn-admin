# Styling Patterns

## When to read

- When you need guidance for class composition or theme variables

## Rules

- Use `cn()` for class merging
- Use `cn()` for conditional styling as well
- Use Tailwind theme variables such as `text-primary` and `bg-accent`
- Use `data-slot` attributes as styling hooks
- Use Lucide React for icons
