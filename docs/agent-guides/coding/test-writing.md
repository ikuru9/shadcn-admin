# Test Writing Patterns

## When to read

- When adding or modifying tests

## Rules

- Use Vitest + Testing Library + MSW
- Use `vi.mock` as the primary mocking pattern
- Write test descriptions in Korean
- Use `data-testid` when needed
- Use `fireEvent` and `waitFor` appropriately
